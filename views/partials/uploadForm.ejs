<body>

    <div id="login-form-wrap">

        <img src="/logo.png" alt="Trulli" width="100" height="100">
        <h2>DESIDOC e-Resource Sharing</h2>
        <h5>Upload Files</h5>
        <form id="login-form" action="/logout" method="post"> <input type="submit" id="logout" value="Logout"></form>
        <form id="login-form" enctype="multipart/form-data" action="/uploadFile" method="post" onsubmit="return showProgress()">
            <input id='files' type="file" name="file" multiple>
            <input type="submit" id="upload" value="Upload">

        </form>
        <div id="progress"></div>
        <span id="status"></span>
        <div id="create-account-wrap">
            <p>Developed and by Maintained DESIDOC</p>
        </div><!--create-account-wrap-->
    </div><!--login-form-wrap-->
    <script>
        function showProgress() {
            console.log("Showing Progress");
            const progress = document.createElement("progress");
            const text = document.createTextNode("Loading...");
            progress.appendChild(text);

            const element = document.getElementById("progress");
            element.appendChild(progress);
        }
    </script>
    <!-- partial -->